import 'package:flutter/material.dart';

class AppelPage extends StatelessWidget {
  const AppelPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("AppelPage"),
    );
  }
}
